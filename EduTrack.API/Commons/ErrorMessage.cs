namespace EduTrack.API.Commons
{
    public static class ErrorMess
    {
        //    /// <summary>
        //    /// {0} đã tồn tại trên hệ thống!
        //    /// </summary>
        //    public static Func<string, string> Duplicated = (field) => string.Format("{0} đã tồn tại trên hệ thống!", field);

        //    /// <summary>
        //    /// {0} không tồn tại trên hệ thống!
        //    /// </summary>
        //    public static Func<string, string> NotFound = (field) => string.Format("{0} không tồn tại trên hệ thống!", field);

        //    /// <summary>
        //    /// Mật khẩu xác nhận không chính xác
        //    /// </summary>

        //    public static string PasswordMismatch = string.Format("Mật khẩu xác nhận không chính xác!");

    }
}
